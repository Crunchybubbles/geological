package io.github.crunchybubbles.geological.registry;

/** Logical registry layer represented in the first validated snapshot. */
public enum DefinitionKind {
  GEOMETRY_KERNEL,
  PROCESS_RULE,
  SCALE_PROFILE
}
